[alias]
ta = ["test", "--all-features", "--workspace"]
tn = ["test", "--no-default-features", "--workspace"]
open-doc = [
    "test",
    "--package",
    "tests",
    "--test",
    "doc",
    "--all-features",
    "--",
    "build_and_open_rust_doc",
    "--exact",
    "--show-output",
    "--ignored",
]
